spring:
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password: 

    jpa:
        open-in-view: false
        show-sql: false
        properties:
            hibernate:
                format_sql: false

    h2:
        console:
            enabled: true
            path: /h2-console